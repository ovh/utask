name: exec-script
description: Executing a python script
title_format: "[test] a simple task"

inputs:
- name: file
  description: file path

steps:
    stepOne:
        description: first step
        action:
            type: script
            configuration:
                file: "{{.input.file}}"
                argv:
                  - world
                timeout_seconds: "25"